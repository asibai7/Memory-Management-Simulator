#ifndef ALGORITHM_H
#define ALGORITHM_H

void fifo(char* fileName, int numFrames, char* setting); //the following headers include the functions and their necessary parameters
void lru(char* fileName, int numFrames, char* setting);
void vms(char* fileName, int numFrames, int percent, char* setting);

#endif